# 過去バージョン保管庫

このディレクトリはビルド対象外です（Astroは `src/` のみを見ます）。
新旧バージョンを見比べるためのスナップショットを置いています。

- `v1/` … 2026-07-26 時点のバージョン（トップページ改修前）。`src/`, `public/`, `astro.config.mjs`, `package.json` のコピー。

v1 をローカルで見たい場合は、リポジトリを別の場所へコピーし、`versions/v1/src` を `src` に差し替えてビルドしてください。
