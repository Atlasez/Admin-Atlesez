# v2 改修メモ（2026-07-26）

旧バージョンは `versions/v1/` に保存済み（ビルド対象外）。

## 指摘いただいた項目への対応

| 指摘                                               | 対応                                                                                                                                                            |
| -------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 運営紹介はアトラス側に不要                         | ヘッダーナビから削除。ページ自体は残しつつ `noindex` を付与（組織サイト側に集約するまでの経過措置）                                                             |
| 「最近更新された記事」の優先度が低い／面積が大きい | PC は右カラムへ寄せ、日付をタイトルと同じ行の右端に配置。高さ 26rem のスクロール領域に変更（表示件数は 5 → 20 件に増やせた）。スマホでは 18rem のスクロール領域 |
| 「現行サイトを見る」を全削除                       | トップ・分野ページ・カテゴリページの外部リンクと関連文言をすべて削除                                                                                            |
| 未公開ジャンルもトップに表示／ボックスが大きすぎる | 公開・準備中を 1 つのグリッドに統合。`<details>` の折り畳みを廃止。タイル 1 個は「分野名＋?＋記事数」の 3 行相当まで圧縮                                        |
| 分野の説明は不要／分野名を大きく                   | 説明は既定で非表示。分野名を 1.3rem・太字に拡大。各タイルの「?」ボタンで「〇〇とは」（＝分野説明）をその場で開閉                                                |
| 全体的に余白が多すぎる                             | デザイントークンの空きを圧縮（後述）                                                                                                                            |
| 記事本文の横幅が狭い                               | 記事レイアウトの左右カラムを 14rem/16rem → 12.5rem/14rem に、`--reading-width` を 43rem → 56rem に拡大。本文カラムは実質 +10rem 程度                            |
| 「はじめての方へ」を上のメニューへ                 | ヘッダーナビに追加（運営紹介と入れ替え）。トップページのボタンは削除                                                                                            |
| 公開記事数・分野数を上部か「分野」見出しの右へ     | 「分野」見出しの右に「7分野・149記事を公開中」として配置                                                                                                        |
| 「利用　無料・広告なし」は不要                     | サマリー枠ごと削除                                                                                                                                              |
| 「分野」より上の部分は不要                         | 見出し・リード文・ボタン群・サマリー枠をすべて削除。トップは「分野」から始まる                                                                                  |
| 旧バージョンを残す                                 | `versions/v1/`（README 付き）                                                                                                                                   |

## 指摘外で変更した点

- **デザイントークンの余白を全面圧縮**（`src/styles/tokens.css`）。`--space-3` 1rem→0.875rem、`--space-4` 1.5→1.25rem、`--space-5` 2.5→1.75rem、`--space-6` 4→2.5rem。全ページに効きます。
- **行間を 1.85 → 1.75 に**。日本語の可読性は保ちつつ、1 画面に入る行数を増やす狙い。
- **`--content-width` を 72rem → 78rem に拡大**。ワイド画面で一覧性が上がります。
- **記事本文の見出し・段落の余白を個別調整**。`h2` 上マージンを space-5→space-4、`h3` と段落にも明示的に狭いマージンを指定（従来はブラウザ既定の 1em 依存でばらついていた）。
- **`main` の上下パディングを space-4 → space-3**。
- **トップページの見出し階層を修正**。従来はサイト名が `h1`、分野が `h2`、分野名が `h3` でしたが、リード部を消したため「分野」を `h1`、「最近更新された記事」を `h2` に。分野名は見出しではなくリンクのリストにしています（スクリーンリーダーでの一覧走査がしやすいため）。
- **英語版のベータ告知文を修正**。「現行サイト」への言及を削除し、日本語版へ誘導する文面に変更。
- **カテゴリページの「準備中」記事の説明文**から「現行サイトの目次に掲載されている」を削除し、「目次に予定されている」に変更。
- **未使用になった CSS を削除**（`.subject-external-link` など）。
- `versions/` を tsconfig / eslint / prettier の対象外に追加。

## 未対応・要相談

- 「最近更新された記事」の日付が全件 2026/7/22 で並んでいます（一括インポート由来）。実際の更新日が入るまで日付の情報価値はほぼゼロなので、いずれ非表示にするか実日付を入れるか決めたいところです。
- 運営紹介ページは残しています。完全に消す場合は `src/pages/atlas/[locale]/team/` を削除し、`tests/e2e/main-pages.spec.ts` と `tests/e2e/accessibility.spec.ts` から該当テストを外す必要があります。
- 分野タイルの「?」は現状 JS でその場に説明を開く方式です。将来「〇〇学とは」の解説記事ができたら、リンク先をその記事に差し替えるのが自然だと思います。

## 検証

- `npm run build` 成功（261 ページ）
- `npx astro check` 0 errors / 0 warnings
- `npm run lint` / `npm test`（unit 11件）成功
- E2E は当環境で Chromium が入らず未実行。ローカルで `npm run test:e2e` の確認をお願いします（ナビ変更に伴う失敗はないはずですが、「総合ホームに分野と準備中の区別がある」テストのみ表示形式が変わっています）

## v2.1 アクセシビリティ修正（CI の axe 検査対応）

初回 CI で `atlas/ja/` と `atlas/en/` の axe 検査が落ちました。原因と対応：

- **`opacity: 0.72` によるコントラスト不足（WCAG 1.4.3, 違反）**
  準備中の分野タイルを `opacity` で淡くしていたため、白背景との実効コントラストが
  分野名 2.88:1 / 「準備中」ラベル 3.36:1 まで落ちていました（要求 4.5:1）。
  `opacity` を廃止し、色は `--text-secondary`（6.34:1）のまま、
  **文字の太さ（500 対 650）と下線の破線化**で公開／準備中を区別する方式に変更。
- **「?」ボタンのターゲットサイズ不足（WCAG 2.5.8, 違反）**
  1.35rem = 21.6px で 24px 未満でした。1.5rem（min 24px）に拡大。
- **分野名リンクのターゲットサイズ（予防的対応）**
  インライン要素で高さが 24px 前後と際どかったため、`inline-flex` + `min-height: 1.75rem` に。
- **「最近更新された記事」の日付の色**を `--text-muted` → `--text-secondary` に変更。
  `--text-muted` は既定の白背景では 4.93:1 で通りますが、表示設定の「低刺激色」背景では
  4.45:1 と基準割れするため。

検証：axe-core を jsdom 上で実行し構造ルールは 0 件、コントラストは各配色の
コントラスト比を計算して確認しました（当環境ではブラウザが動かないため実ブラウザでの
axe 実行は未実施）。

## v2.2 記事の要約（summary）の作り直し

「記事が途中で切れている」というご指摘への対応。本文は無傷で、壊れていたのは
frontmatter の `summary` でした。

**原因**：旧インポータの `makeSummary` が、本文の冒頭 110 字を機械的に切り出して
いました。その際にインライン数式 `$...$` を空白へ置換していたため、数式が抜けた
文が途中の「…」で終わる要約が量産されていました（153 件中 113 件）。
加えて、文末判定のとき正規表現が末尾の空白まで取り込んでいたため、文として
完結しているのに余分な「…」が付くケースもありました。

**対応**

- `makeSummary`（`scripts/import-utils.mjs`）を書き直し
  - 記事冒頭と最初の節だけを見る（奥から拾うと文脈のない一文が要約になるため）
  - 見出し・表・箇条書き・別行立て数式の段落は捨てる
  - インライン数式を含む文は捨てる
  - 「定義 1 (…)」のような定理環境の見出し、「これは〜」のような前文脈を受ける文、
    助詞で始まる断片（数式で分断された残骸）も捨てる
  - どれも取れない場合はタイトルから定型文を作る
- `scripts/regenerate-summaries.mjs` を追加し、既存 113 件を再生成
  （末尾が「…」の要約だけが対象。手書きの要約 40 件は触っていません）
- 45 件は本文から自然な一文が取れました。残り 68 件は定型文です。
  対象記事は `docs/SUMMARY_TODO.md` に一覧化しました
  （`node scripts/regenerate-summaries.mjs --list` で作り直せます）
- **記事ページのタイトル直下の要約表示を廃止**。本文冒頭と重複しがちなためです。
  一覧ページ・検索結果・`meta description`・OGP では引き続き使います

**検証**：113 ファイルについて、本文と `summary` 以外の frontmatter が
1 バイトも変わっていないことを確認済み。

## v2.3 Cloudflare Pages への配信切り替え

配信先を GitHub Pages から Cloudflare Pages に移した。

- **`astro.config.mjs`**: `SITE_URL` → `CF_PAGES_URL` → `http://localhost:4321`
  の順にフォールバック。`SITE_URL` を Production 環境にだけ設定すれば、
  プレビュー配信は自分自身の URL を canonical に使う。
  既定値だった `https://atlasez.github.io` は誤ったURLが本番に出る事故のもとなので撤去
- **プレビュー配信の noindex 化**（`src/lib/deploy.ts`）。`CF_PAGES_BRANCH` が
  `main` 以外なら全ページに `noindex, nofollow` を付け、`robots.txt` も
  `Disallow: /` にする。本番と同じ内容が二重にインデックスされるのを防ぐ
- **`robots.txt` を動的生成に変更**（`public/robots.txt` → `src/pages/robots.txt.ts`）。
  従来は sitemap の URL が GitHub Pages のものにハードコードされていた
- **`public/_headers` を追加**。`nosniff` / `Referrer-Policy` /
  `X-Frame-Options` / `Permissions-Policy` / `Cross-Origin-Opener-Policy` と、
  `/_astro/*` の 1 年 immutable キャッシュなど
- **`.nvmrc`（22）を追加**。Cloudflare Pages と GitHub Actions で
  Node のバージョンを 1 箇所から与えるため。ci.yml も `node-version-file` に変更
- **`ci.yml` の deploy ジョブを削除**。GitHub Actions は検証専用になった。
  あわせて検証時の `BASE_PATH` を `/<リポジトリ名>` から `/` に変更し、
  本番と同じパス構成でリンク検証・E2E が走るようにした
- `docs/DEPLOYMENT.md` と `docs/PUBLISH.md` を Cloudflare Pages 向けに書き直し

### 残っている手作業（Cloudflare のダッシュボード側）

1. Build command `npm run build` / Output directory `dist` の確認
2. Production 環境変数に `SITE_URL` を設定 → **Retry deployment**
3. GitHub の Settings → Pages → Source を「None」に戻す

## v2.4 漢字記事の一字表の組み直し

「読みがずれている・空きがある」というご指摘への対応。

**原因**：元データ（Google Sites）の表は読みのセルが**縦に結合**されていた。
Markdown へ変換した際に

1. 先頭の実データ行がそのまま Markdown のヘッダ行になり、
   1 文字目の漢字が列見出しとして描画されていた
   （例: 「火 | ひ | ほ」が表のヘッダになっていた）
2. 結合セルだった箇所が空セルや欠けた列として残り、表に穴が空いていた
   （例: `| 燬 |  |` `| 焔 |`）

**対応**：`scripts/fix-kanji-tables.mjs` を追加し、結合セルの意味どおりに
読みを上の行から引き継いで 2 列（漢字・読み）の表に組み直した。

```
| 火 | ひ | ほ |        →   | 漢字 | 読み   |
| --- | --- | --- |         | ---- | ------ |
| 燬 |  |                   | 火   | ひ、ほ |
| 炎 | ほのお |              | 燬   | ひ、ほ |
| 焔 |                      | 炎   | ほのお |
                            | 焔   | ほのお |
```

- 対象は漢字記事 78 件のうち 77 件・表 278 個
  （`nature/sky.md` だけ以前に手で直されていたので対象外）
- 複数列に分かれていた読みは「、」で連結（セル内では「・」が使われているため）
- 変換済みの表は触らないので、スクリプトは何度実行してもよい

**検証**

- 元データの漢字セル 1916 個と変換後の行 1916 行が一致（欠落ゼロ）
- 元データから前方補完で作った期待値 1916 組と、変換後の（漢字, 読み）が**完全一致**
- 読みが空のまま残った行 0 件
- frontmatter と表以外の本文が 1 行も変わっていないことを diff で確認

### 未対応

【熟語】欄は「炎炎炎焼炎上火炎」のように区切りなしで連結されている。
元データにも区切りがなく、語の境界を機械的に判定できないため手つかず。
