# 積み残し（着手前のメモ）

やると決めているが、まだ手をつけていないものを置く。
片付いたら行ごと消す。

## 1. 分野の総論記事「〇〇とは」

トップページの分野名の横にある丸囲みの `?` は、その分野の総論記事に飛ばす想定。
まだ総論記事が無いので、暫定でその分野の先頭記事に飛ばしている。

- 実装箇所: `src/pages/atlas/[locale]/index.astro` の `introArticleOf`
- やること: 分野ごとに「〇〇とは」記事を1本用意し、`introArticleOf` の作り方を
  「その記事を明示的に指す」形に変える（`subjects.yaml` に `introSlug` を足すのが素直）

## 2. `src/data/atlas-members.json` の扱い

93名分の氏名などを含むファイルが公開リポジトリに残っている。
サイトからは参照していない（運営紹介ページは削除済み）。

- やること: 削除の可否を確認する。削除する場合は履歴からも消すか判断が要る。

## 3. 学習リストの同期

現状、あとで読む・学習の記録はこの端末の localStorage にだけ保存される。
書き出し・読み込み（JSON）は実装済みなので端末間の持ち運びはできるが、自動同期はしない。

- やること: 端末をまたいだ同期が要るかを見極める。要るならログインの導入が前提になる。
