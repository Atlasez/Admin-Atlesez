# 運営サイト UI/UX 改善・信頼性向上 Goal

あなたはプロダクトデザイナー、フロントエンドエンジニア、QA エンジニアを兼ねる。運営サイト全体を実際に調査し、既存機能を壊さず、問題の発見から実装・検証・本番確認まで完了させる。見た目だけのモックや未接続のボタンで完了扱いにしない。

## 目的

管理トップ、ポータル、アクションセンター、タスク、カレンダー、記事一覧、記事編集、目次、分野・カテゴリ、権限、メンバー、応募、承認、操作履歴、開発者・監査など、運営サイトの全ページを一貫した業務ツールへ整える。最優先は usability、clarity、correctness、consistency、performance、accessibility、visual polish、decoration の順とする。

## 最初に行う調査

1. `src/pages/admin`、`src/components`、`src/layouts`、`src/styles`、API、DB、テスト、GitHub Actions、Cloudflare 設定を一覧化する。
2. Chrome の PC 表示（幅 1440px 前後）、タブレット、スマートフォンで主要ページを開き、初期表示、読み込み中、成功、空、失敗、再試行、権限不足、テーマ切替、キーボード操作をスクリーンショットで保存する。
3. ページごとの重複機能、壊れたリンク、横並び崩れ、重なり、レイアウトシフト、白漏れ、担当外データ混入、英語の補助見出し、過剰な余白・カード・左アクセント線を記録する。
4. 既存の共通コンポーネントとデザイントークンを優先して再利用し、実装前後の差分を表にする。

## UI/UX の必須方針

- 日本語のページ名・見出しだけを表示し、青い英語 eyebrow、kicker、サブ見出し、AI風の説明ラベルは追加・復活させない。
- 紫/青のグラデーション、装飾的グラデーション、glassmorphism、glow、巨大な角丸、過剰な影、不要なバッジ・アイコン・カード・余白は禁止する。
- 色は既存トークンを使う。新色を追加せず、ポータルとアクションセンターの件数は `#52A450`（`--task-count`）へ統一する。
- 太い青い左線は廃止し、状態は境界、背景、短いラベル、テキストで示す。意味のある警告だけ既存の semantic token を使う。
- 通常ボタンは高さ 32px、主要フォームのみ 36px、40px 超は特殊用途以外で禁止。入力、select、checkbox、radio、switch の寸法を共通化する。
- 4px 基準の余白（4/8/12/16/20/24/32）を使用し、一覧性を優先する。ページ切替前後で高さと中央位置が変わらないよう領域を予約する。
- すべての動的領域に `aria-busy`、共通ローディング、空状態、エラー、再試行を実装する。再試行は同じ領域内に置き、エラー文言を具体的な日本語にする。

## 機能要件

### ポータルとアクションセンター

- 要対応、期限間近、未読、承認待ち、次回予定をコンパクトなサマリーで表示する。
- API の実データと件数を同じ権限フィルタで取得し、承認待ち件数の不一致をなくす。0 件は「0」と明示し、推測値・古いキャッシュを表示しない。
- アクションセンターは文字列の列挙をやめ、種別、優先度、期限、担当、状態、主操作を持つ行/カードにする。完了済みは既定で非表示、履歴フィルタで確認できる。

### タスク・カレンダー

- チェックボックスは 16px 前後の視認可能なサイズに統一し、44px の操作ターゲットを確保する。
- 大規模一覧はカーソルページングまたは「さらに読み込む」で取得し、初回に全件を描画しない。重複・順序逆転・二重リクエストを防ぐ。
- 曜日単位の可否登録を日付範囲と矛盾なく保存し、タイムゾーンを明示する。

### 記事一覧・記事編集・公開

- 担当範囲を API と UI の両方で制限し、担当外の分野・プロジェクト・公開記事を返さない。担当範囲確認中は分野選択を無効化する。
- フィルタを保存し、リセットを提供する。更新案作成中などの状態を一覧上で明示する。
- 公開記事の本文はロックし、編集時は更新案作成を促す。自動保存 OFF では保存ボタン以外で永続化しない。
- 接続状態を「接続中 / 同期中 / 一時切断 / 再接続中 / 同期完了 / 保存失敗」に分ける。再接続は指数バックオフ、保存要求の重複排除、古い更新の上書き防止を行う。
- 公開操作は検証→PR 作成→CI→マージ→学習サイト反映の各段階を追跡し、失敗理由、再試行、手動復旧導線を画面に出す。権限差（Checks API/commit status、GitHub App/PAT）を吸収する。

### 目次・分野・カテゴリ

- 分野、カテゴリ、中カテゴリ、記事を一画面の構造ツリー/タイルで俯瞰できるようにする。
- 新規記事作成時に「記事から作成」「目次から作成」を選べる。目次から項目追加→タイトル編集→並び替え→記事執筆→公開まで実際に接続する。
- ドラッグ＆ドロップ、キーボード並び替え、アーカイブ、復元、一括並び替えを実装し、順序とタイトルを学習サイトへ反映する。slug は自動生成し、必要時だけ編集できる。
- 新しい分野・カテゴリを追加し、権限と担当者を同時に設定できる。

### 権限、メンバー、監査、開発者モード

- API の権限判定を `requireAdminScope()` 相当へ統一し、UI 非表示だけに依存しない。
- 初回作成者と編集者は表示名・正しいアバターで表示する。
- 公開、承認、権限変更、メンバー削除、記事更新を監査ログへ記録し、誰が・いつ・何を変更したか検索できるようにする。
- 操作履歴、状態整合性、配信/ビルド診断、アップデート履歴、UI プロトタイプ、オンボーディングデモは Developer Mode（運営内運営と `UKYOUKAY0@gmail.com` のみ）へ移し、通常メニューから目立たせない。削除ではなくアーカイブ/検証領域へ移す。

## パフォーマンス

- ページ初期 HTML を軽くし、記事本文・CodeMirror・KaTeX・Yjs など編集専用依存は編集画面で遅延ロードする。
- 一覧はカーソル API、必要な列だけの SELECT、AbortController、短い TTL キャッシュを使い、同一 URL の同時取得を 1 本にまとめる。
- 読み込み中から成功への CLS を 0 に近づける。Chrome DevTools で LCP、CLS、TBT を測定し、変更前後を記録する。

## アクセシビリティとレスポンシブ

- セマンティック HTML、見える `:focus-visible`、キーボードで全操作、適切な label/ARIA、コントラスト WCAG AA、タッチターゲット 44px を満たす。
- デスクトップはサイドバー/ヘッダーを維持、タブレットはナビを折りたたみ、モバイルは一覧を単列、表は横スクロールまたはカード化、ダイアログは画面内に収める。
- `prefers-reduced-motion` では回転・点滅・状態アニメーションを停止する。

## 検証と完了条件

1. 主要ページを Chrome で再現し、PC/タブレット/モバイルのスクリーンショットを変更前後で比較する。
2. `npm run lint`、`npm test`、`npm run check`、ビルド、内部リンク検証、Playwright + axe、Lighthouse を実行する。
3. 権限境界、空/失敗/再試行、ページング、公開失敗からの復旧、目次から記事作成、テーマ切替を E2E で確認する。
4. PR を作成し、CI が成功してから Merge する。Merge 後に本番 `https://admin.atlasez.org/build-info.json` の commit が Merge commit と一致すること、主要 URL が 200 であることを確認する。
5. 最終報告には、洗い出した問題、修正内容、未解決のリスク、スクリーンショット、テスト結果、PR、Merge commit、本番反映時刻を記載する。

## AI エージェントへの制約

作業開始前に `DESIGN.md` と本 Goal を読む。既存コンポーネント・トークン・API を優先し、新しい色、余白、radius、ページ固有パターンを勝手に増やさない。似た画面と同じ操作を別実装にしない。見た目を派手にすることを改善とみなさない。変更後は全ページ、全テーマ、全権限、読み込み/エラー/空状態との一貫性を確認し、未接続の UI や未検証の推測で完了報告をしない。
