# Atlasez UI Design System

この文書は、学習サイト（Atlas）と運営サイト（Admin）の今後のUI実装に適用する規範である。新規画面、既存画面の改修、プロトタイプの更新はこの文書を正本とする。既存画面には移行前の例外が残るため、既存値を無断で一斉変更せず、新規実装から以下の規則に合わせる。

## 適用範囲と参照優先度

参照の優先順位は次の通り。

1. 提示されたCodexスクリーンショット：全体の色、コントラスト、タイポグラフィ、密度、意味色。
2. Raycast Dark：小さな意味色の輝度・彩度とニュートラル階層のみ。
3. Linear：リスト、テーブル、ページヘッダー、情報構造のみ。

参考UIのロゴ、固有グラフィック、装飾はコピーしない。参考間で競合した場合はCodexの雰囲気と意味色を優先し、Linearは構造の参考に限定する。

### 現行実装の調査結果

- CSSカスタムプロパティは `src/styles/tokens.css`、全体基礎は `src/styles/base.css`、記事本文は `src/styles/article-content.css` にある。
- 共通部品は `BaseHead`、`AdminNav`、`SettingsMenu`、`A11ySettings`、`ThemeToggle`、`LangSwitch`、`HeaderSearch`、`HistoryStageControl`、`LearningMap`、`Breadcrumb`、`UiLanguage` を優先して再利用する。
- `tailwind.config.*` は存在しない。Tailwindの任意値を導入せず、CSSカスタムプロパティをトークンの正本にする。
- アイコン専用ライブラリは依存していない。既存のインラインSVGを優先し、必要な場合だけ同じviewBox・線幅のSVGを追加する。
- `/admin/ui-prototype/` は独立した検討用ページであり、通常のナビゲーションや既存業務フローに依存しない。削除時はそのディレクトリと専用静的アセットだけを対象にする。
- 現在の `tokens.css` の明色、暗色、ハイコントラスト、縮小モーション設定は互換性のため維持する。下記の値は新規実装の標準であり、移行時は既存変数（`--background-primary` 等）へ別名を張って段階的に合わせる。
- `AdminNav` の古いアバター用グラデーションや一部ページの大きなカード・強い影はレガシー例外であり、新しいパターンとして複製しない。

### 画面・パターンの観察

- `admin/portal`、`admin/atlas`、`admin/articles`、`admin/review`、`admin/reports` は一覧・フィルター・状態表示を主とする。見出し、操作バー、連続行を同じ本文軸に揃え、行をカードの集合にしない。
- `admin/genres`、`admin/analytics`、`admin/permissions`、`admin/applications` は管理表と編集フォームを組み合わせる。表を主表示にし、タイルは参加者の比較など、表では関係が見えにくい場合だけ使う。
- `admin/calendar`、`admin/member-calendar`、`atlas/[locale]/map` は操作密度が高い。ツールバーを36pxに統一し、凡例・ズーム・表示設定をpopoverまたはDisclosureに収める。
- `admin/editor` は本文、コメント、プレビュー、コード・数式設定を持つ多ペイン作業面である。本文を最も明るい主面にし、補助面は折りたたみ・リサイズ可能な補助機能として扱う。行番号、コメントの固定ヘッダー、ペインハンドルは常時利用できる。
- `atlas/[locale]/index`、`list`、`search`、`[subject]/[category]/[slug]` は学習者向けの読み取りフローである。記事本文は `--reading-width` と1.75行高、管理情報は12–14pxのdense UIで分離する。
- `AdminNav` は約64px高、ロゴ約34px、ナビ文字13px前後の既存パターンを持つ。ヘッダーを大きくせず、メイン本文の開始位置を揃える。
- Codex参考画像では、サイドバー（約`#101112`）が作業面（約`#151515`）より後退し、標準面（約`#191919`）と浮上面（約`#1d1d1d`）が近接している。明るい本文、抑えたメタデータ、薄いdivider、小さく鮮やかな状態アイコンが情報階層を作る。
- 「要対応」参考画像では、数値カウンターは20–28pxで、青・赤・黄の意味色を数字と小さな枠内アイコンにのみ使っている。行の背景はneutralのままである。この局所コントラストを標準とする。

## Design Principles

### 1. 内容を最優先する

画面の主目的（読む、編集する、確認する、送信する）を最も明るく、最も広い連続領域に置く。装飾を追加する前に、同じ情報を余計な枠なしで配置できるか確認する。

### 2. ニュートラルを基調にする

画面面積の90–95%は低彩度のニュートラル面とする。青・紫の面で選択状態を塗りつぶさない。意味色はアイコン、点、数値、2px以内のマーカーに限定する。

### 3. 隣接面の差で階層を作る

主背景、サイドバー、面、浮上面は隣接輝度差をおおむね2–8%に抑える。強い罫線や影で区切る前に、余白、文字階層、面の差で構造を伝える。

### 4. 密度は機能で決める

「余白が多いほど高級」とは扱わない。一覧・タスク・応募管理などは1画面に必要な情報を収める。タッチ可能な最小領域を確保したうえで、装飾的な余白を削る。

### 5. 状態は色だけにしない

成功・警告・失敗・公開状態は、色に加えてラベル、アイコン、形、またはテキストで示す。色覚差で意味が失われないことを必須とする。

### 6. 既存部品を拡張する

似た部品がある場合はそのAPI、寸法、状態、トークンを再利用する。ページ専用のボタン、入力、タブを作る前に共通部品への拡張を検討する。

### 7. 予測可能な操作を優先する

リンクは遷移、ボタンは状態変更、タブは同一文脈の表示切替に使う。操作後にフォーカス、スクロール位置、選択状態を失わせない。

### 8. 仕上げは局所的に行う

全体をカラフルにするのではなく、重要な状態の局所コントラストだけを上げる。アニメーション、影、バッジは理解に寄与する場合のみ追加する。

## Visual Personality

### 目指すもの

- Codexのような、温かみのある低彩度ニュートラル面。
- 明るい本文と明確に抑えたナビゲーションの差。
- 小さいが鮮やかな黄色、緑、赤、青、紫、シアンの意味色。
- 情報密度が高く、静かで、業務ツールとして信頼できる画面。
- 4–8pxを基準に揃った寸法、控えめな境界、短い状態遷移。

### 目指さないもの

紫・青のグラデーション、未来的なAI演出、ガラス風半透明面、発光、ゲームUI、Discord風の大きな色面、マーケティングサイトのヒーロー、巨大見出し、過剰なカード、過剰な丸み、過剰な余白。

## Design decision priority

ルールが競合したときは次の順で判断する。

1. usability（操作できる、読める）
2. clarity（状態と目的が明確）
3. consistency（共通部品・トークンとの一貫性）
4. information hierarchy（重要度の差）
5. density（必要情報量）
6. visual polish（微調整）
7. decoration（装飾）

## Color System

### トークンのルール

色は以下のトークンだけを使う。新しいhex、rgb、oklchをコンポーネント内に直書きしない。意味色はOKLCHを優先し、同じ役割で明暗テーマの輝度だけを調整する。ニュートラル面には青・紫の色相を混ぜない。

### 暗色（Codex基準、標準の運営UI）

```css
--bg-primary: #151515; /* 作業キャンバス */
--bg-sidebar: #101112; /* ナビゲーション */
--surface: #191919; /* 標準面 */
--surface-raised: #1d1d1d; /* 入力・浮上面 */
--bg-hover: #202020;
--bg-selected: #242424; /* 選択は無彩色 */
--text-primary: rgb(255 255 255 / 0.92);
--text-secondary: rgb(255 255 255 / 0.63);
--text-tertiary: rgb(255 255 255 / 0.44);
--text-placeholder: rgb(255 255 255 / 0.36);
--border-subtle: rgb(255 255 255 / 0.05);
--border-default: rgb(255 255 255 / 0.08);
--border-strong: rgb(255 255 255 / 0.12);
--accent-primary: oklch(75% 0.16 255); /* リンク・主操作 */
--accent-strong: oklch(82% 0.14 255);
--focus-ring: oklch(82% 0.14 255);
```

`--bg-sidebar` は `--bg-primary` より暗く、`--surface` は作業キャンバスより2–4%明るくする。`#08090A`のような純黒に近い大面積は使わない。選択行は `--bg-selected` と明るい文字だけで表し、青・紫の背景を使用しない。

### 明色（Atlas既存値を尊重）

```css
--bg-primary: #f7f7f5;
--bg-sidebar: #f1f1ef;
--surface: #ffffff;
--surface-raised: #fbfbf9;
--bg-hover: #f4f4f2;
--bg-selected: #ececea;
--text-primary: #20201e;
--text-secondary: #6b6b67;
--text-tertiary: #92928c;
--border-subtle: #e9e9e5;
--border-default: #deded9;
--border-strong: #bdbdb7;
--accent-primary: #176ea6;
```

### 意味色（小面積のみ）

以下は方向値であり、知覚輝度を揃えるためOKLCHを使う。暗色では彩度を下げず、文字・線が背景から明確に分離する値を採用する。

```css
--semantic-blue: oklch(78% 0.2 259); /* 情報・タスク */
--semantic-green: oklch(80% 0.19 150); /* 公開・成功 */
--semantic-yellow: oklch(83% 0.19 85); /* レビュー待ち */
--semantic-amber: oklch(77% 0.2 62); /* 期限・注意 */
--semantic-red: oklch(72% 0.2 25); /* 問題・失敗 */
--semantic-violet: oklch(77% 0.22 300); /* 担当・協働 */
--semantic-cyan: oklch(80% 0.16 195); /* リアルタイム・システム */
```

意味色はステータス点、16–20pxアイコン、数値、差分、2pxの左線、短いラベルにのみ使用する。大きな背景、全面グラデーション、カード全体の塗りつぶしには使わない。色面積が画面の10%を超えたら、色を薄めず面積を減らす。意味色テキストは通常 `font-weight: 600` 以下とし、白との併記やラベルで補強する。

### 既存トークンとの対応

`src/styles/tokens.css` の `--background-primary/secondary/inset`、`--text-primary/secondary/muted`、`--border-default/strong`、`--accent-primary/hover/subtle`、`--link`、`--focus-ring` を上記の役割に対応付ける。既存のAtlas青、記事directiveの黄色・シアン・赤線は意味色の局所使用として維持できる。新規変数を作る場合はこの役割表に追加してから使用する。

## Typography

### フォント

- UI本文：`var(--font-current)`。既存の日本語フォールバック（Hiragino Sans / BIZ UDPGothic / Yu Gothic / Noto Sans JP / system-ui）を維持する。
- コード・数式入力：`var(--font-mono)`。数式表示はKaTeXを優先する。
- 英語UIだけのためにInterを追加しない。プロトタイプのInter風指定は日本語フォールバックを含む既存UIと整合させる。

### サイズ、ウェイト、行高

| 用途             |  サイズ | ウェイト | 行高 |
| ---------------- | ------: | -------: | ---: |
| 記事本文         |    16px |      400 | 1.75 |
| UI本文           |    14px |      400 | 1.55 |
| dense admin本文  |    13px |      400 | 1.45 |
| ページ見出し     | 24–28px |      600 | 1.25 |
| セクション見出し | 18–20px |      600 |  1.3 |
| 小見出し         | 15–17px |      600 | 1.35 |
| ラベル・ボタン   |    13px |  500–600 |  1.3 |
| メタデータ       |    12px |  400–500 | 1.35 |
| キャプション     |    11px |      400 | 1.35 |
| テーブル         | 12–13px |  400–600 | 1.35 |
| コード           | 13–14px |      400 |  1.5 |
| 数値カウンター   | 20–28px |      600 |  1.1 |

`h1` は32pxを上限とし、ヒーロー用途の48px以上は禁止する。ナビゲーションは500–600、見出しは原則600とし、800はロゴなど既存のブランド例外だけにする。日本語本文は1.6未満にしない。文字を小さくして密度を稼がず、まず余分な説明や列を減らす。

## Spacing System

4pxを最小単位とする固定スケールだけを使う。

| トークン   |   値 | 主な用途                     |
| ---------- | ---: | ---------------------------- |
| `space-1`  |  4px | アイコンと文字、密な行内     |
| `space-2`  |  8px | コントロール内、リスト内     |
| `space-3`  | 12px | セル、ラベル、ボタン左右     |
| `space-4`  | 16px | コンポーネント内、標準カード |
| `space-5`  | 20px | 小セクション間               |
| `space-6`  | 24px | セクション間、ページ内余白   |
| `space-7`  | 32px | 大セクション間               |
| `space-8`  | 40px | ページヘッダーの上下         |
| `space-9`  | 48px | 特殊なページ境界             |
| `space-10` | 64px | 読み物の大きな区切りのみ     |

コンポーネント内部は8/12/16px、コンポーネント間は12/16px、セクション間は24/32px、ページ端はデスクトップ24px・モバイル16pxを基本とする。32pxを超える内側padding、64pxを超える連続空白は、記事本文の可読性など理由を記録した場合だけ許可する。

## Layout

- アプリシェル：`sidebar 236px + workspace minmax(0, 1fr)`。サイドバーはデスクトップで220–248pxの範囲、折りたたみ時56px。
- ヘッダー：高さ64px（既存 `AdminNav` の約66pxは互換範囲）。主要操作を中央線上に揃える。
- dense adminの最大幅：1200px。広い管理表でも1280pxを上限とする。
- 記事本文の読み幅：`56rem`（896px）を上限。本文を画面全幅に伸ばさない。
- ページpadding：デスクトップ24px、タブレット20px、モバイル16px。現行画面の20–28pxはこの範囲で扱う。
- ブレークポイント：`1200px`（広い表の列調整）、`980px`（ナビ・2列解除）、`760px`（サイドバー縮小・フォーム1列）、`640px`（モバイル表・ダイアログ）。端末名ではなく幅で判定する。
- コンテンツは左端の本文軸に揃え、操作バーだけを右寄せにする。見出し、フィルター、表の開始位置を同じ軸に置く。
- 密な画面では2列レイアウトとテーブルを優先し、同一情報をカードと表で重複表示しない。

## Surface Hierarchy

階層は `bg-sidebar → bg-primary → surface → surface-raised → popover/dialog` の順とする。隣接面の差、余白、文字階層を先に使い、1px境界は補助にする。

### カードを使う場合

独立した単位で、タイトル・状態・操作をまとめる必要があり、一覧行では表現しにくい場合だけ使う。通常は1px境界、`surface`、radius 6px、影なし。1セクション内でカードを3段以上ネストしない。

### カードを使わない場合

ナビゲーション、連続する記事一覧、テーブル、タイムライン、設定項目の各行はカード化しない。`surface`の連続面、行間、薄いdividerでまとめる。装飾目的の空カードを作らない。

## Borders

- 幅は1pxを基本とする。2pxはfocus ring、意味色マーカー、選択されたタブの下線だけ。
- subtle：`rgb(255 255 255 / .05)`（明色は `#e9e9e5`）。
- default：`rgb(255 255 255 / .08)`（明色は `#deded9`）。
- strong：`rgb(255 255 255 / .12)`。入力のエラー・重要な外周以外で常用しない。
- dividerは行間の薄い1px線。表の全セルを囲むグリッドは禁止。
- focusは2px相当の高コントラストリング、`outline-offset: 2px`。色をborderだけに頼らない。
- selected borderはローカルなタブ・セグメントに限りアクセント色を許可する。サイドバー選択はneutral面のみ。

## Border Radius

固定値は `sm: 4px`、`md: 6px`、`lg: 8px`、`dialog: 10px`。12px超は原則禁止。pill（999px）はタグ、ステータス、アバター、トグルのトラックなど意味上必要な場合だけ許可する。記事directiveの左上・右下だけのradiusは本文パターンとして維持できる。

## Shadows

通常のカード、一覧、サイドバーには影を付けない。popover/dropdownは `0 8px 24px rgb(0 0 0 / .20)`、dialogは `0 16px 48px rgb(0 0 0 / .28)` を上限とする。発光、色付き影、複数影の重ね掛けは禁止する。影より面差とborderを優先する。

## Buttons

すべてのボタンはネイティブ`button`を使い、ラベルは動詞中心にする。

| 種類        |                       高さ | padding | 面・文字                 | 用途                 |
| ----------- | -------------------------: | ------: | ------------------------ | -------------------- |
| Primary     | 36px（フォーム内40pxまで） |  0 14px | accent面 / on-accent     | 画面の主操作1つ      |
| Secondary   |                       36px |  0 12px | raised面 / primary       | 補助操作             |
| Ghost       |                    32–36px |  0 10px | 透明 / secondary         | ツールバー、軽い操作 |
| Destructive |                       36px |  0 12px | 原則透明＋red文字/border | 削除・取消           |
| Icon button |  32–36px（ヒット領域44px） |     8px | neutral面                | 検索、閉じる等       |

radiusは通常6px、compactは4px。hoverは面を1段明るく、activeはさらに1段暗くする。色の彩度やサイズを変えて押下を表さない。disabledはopacity .45、`cursor: not-allowed`、それでもラベルは読めるようにする。focusは共通ring。Primaryは1つの主要目的に限定し、破壊操作に大きな赤面を使わない。

## Form Controls

- input/select/textareaの標準高さ36px、主要フォーム40px、dense32px。左右paddingは12px（dense8px）。
- radius 6px、背景 `surface-raised`、border default。placeholderは `text-placeholder`。
- textareaは最低3行・resize縦のみ。selectはネイティブキーボード操作を保持する。
- labelは13px/500、controlとの間隔4–8px。補助説明は12px/`text-secondary`で1行を基本とする。
- checkbox/radioの視覚部は16px、switchは36×20px。ただしクリック可能な透明ヒット領域は44pxを確保する。
- switchは設定の二値状態だけに使用し、選択肢一覧にはcheckbox/radioを使う。
- errorは赤いborderだけでなく、隣接テキスト（12–13px）と`aria-describedby`で示す。送信中はボタンを無効化し、進行表示を同じ位置に置く。

## Navigation

- サイドバー幅236px、折りたたみ56px。項目高さ32px（タッチ主体は36px）、左右padding8–12px、項目間2–4px。
- アイコンは16px、ラベルは13–14px/500。セクション見出しは11–12px/600のmuted、上下間隔16px。
- hoverは`bg-hover`、selectedは`bg-selected`のみ。selectedの文字はprimary、アイコンは原則neutral。意味のあるカウンター・状態点だけsemantic color。
- header navigation項目は36px高、現在位置は下線または文字weightで示す。大きな青い帯を置かない。
- collapsed時はラベルを隠し、`aria-label`とtooltipを付ける。順序とキーボード移動を変えない。
- 区切りはセクション間の余白を優先し、強い線を連続させない。

## Tables / Lists

- dense row 40px、標準row48px、リッチな操作row56px。セル左右padding8–12px、上下8px。
- 見出しは12px/600、本文12–13px、メタデータ11–12px。数値列は右揃え、`font-variant-numeric: tabular-nums`。
- 行hoverは`bg-hover`、selectedは`bg-selected`。dividerはsubtle 1pxを行間だけに置く。
- 行全体をクリック可能にする場合は、内部のリンク・ボタンと競合させない。行アクションはhoverだけでなくfocus時にも表示する。
- ソート・フィルター状態は文字、アイコン、`aria-sort`/`aria-pressed`で示す。ヘッダーを過度に大文字化しない。
- empty stateは80–120px程度の高さ、短い説明1行と主操作1つ。大きなイラストやカードは置かない。
- モバイル640px未満は横スクロール可能な表または優先列だけの積み上げ表示にする。列を画面外で切り捨てない。

## Icons

専用ライブラリは現状ないため、既存のインラインSVGを第一選択とする。追加SVGは24×24 viewBox、線幅1.75–2、round linecap/linejoinを基本とする。

- small 14px、default 16px、emphasis 20px、単独説明24px。
- 文字の中央線に揃え、アイコンとラベル間は4–8px。
- icon-only buttonには必ず可視tooltipまたは`aria-label`を付ける。タイトルだけに依存しない。
- 装飾目的、全ラベルへの機械的付与、絵文字の代用は禁止。アイコンだけで意味を推測させず、必要なら短いラベルを併記する。
- semantic colorは状態を表すアイコン・点に局所使用し、アイコン周囲を大きく着色しない。

## Interaction States

| 状態     | ルール                                               |
| -------- | ---------------------------------------------------- |
| default  | tokenの標準面・文字・border                          |
| hover    | 面を1段（約4%）明るく。pointer環境のみ               |
| active   | hoverより1段暗く、位置やサイズは変えない             |
| selected | `bg-selected`＋primary文字。色面で選択を表さない     |
| focus    | 2px高コントラストring＋offset2px                     |
| disabled | opacity .45、情報は読める、操作対象から除外          |
| loading  | 12–16pxのspinnerまたはprogress、ラベルと幅を維持     |
| error    | redの局所線/アイコン＋明示テキスト。点滅や揺れは禁止 |

## Component Recipes

### Disclosure / details

任意の補助設定は閉じた状態を初期値とする。行高36–40px、タイトル、短いヒント、chevron（または`+/-`）を同一行に置く。クリック範囲は行全体、`aria-expanded`と`aria-controls`を付ける。エラーや必須入力を閉じて隠さない。記事編集のコードロック、数式設定、執筆メモはこの形式でツールバーを占有しない。

### Tabs

同じ文脈で相互排他的なビュー（記事一覧/表、プレビュー/編集）だけに使う。`role=tablist`、`role=tab`、`aria-selected`、`aria-controls`を実装し、選択タブはneutral面または1–2px下線で示す。グローバル遷移をタブで置き換えない。タブ切替で入力内容、スクロール、フォーカスを不意に失わない。

### Dialog / modal

ネイティブ`<dialog>`を優先し、`aria-labelledby`、Escで閉じる、背景クリックの扱い、開いたボタンへのfocus returnを実装する。デスクトップ最大幅560px、モバイルは必要な場合のみ下部sheet（幅100%、高さ80vh以下）。必須エラーはdialog外にも通知する。

### Dropdown / popover

起点にアンカーし、画面端で反転する。最大高さ60vh、内部スクロール、Escと外側クリックで閉じる。表示中に起点を失わない。メニュー項目は32–36px、キーボード上下移動と`role=menu`/`menuitem`（または適切なlistbox）を使い分ける。

### Toast / notification

`role=status`または`aria-live=polite`の短い1メッセージ。成功・保存確認など非ブロッキング通知だけに使い、重要なエラーをtoastだけで済ませない。連続通知はまとめ、閉じる操作を提供する。

### Loading / error / empty

Loadingは最終レイアウトと同じ寸法のプレースホルダーを置く。Errorは原因、再試行、問い合わせ先の順で短く示す。Emptyは「何もない」理由と次の1操作を同じ領域に置き、巨大なイラストを使わない。

### Article / editor workspace

編集画面は本文を主ペインに置き、コメント・プレビュー・設定を補助ペインに分離する。ペイン操作とリサイズハンドルは16px以上の視覚線＋44pxのヒット領域。コードエディタは等幅13–14px、行高1.5、行番号を本文と同じスクロール軸に固定する。LaTeX補助、コードロック、執筆メモはDisclosureにまとめ、常時表示のタブや大きな色面を作らない。コメント一覧のヘッダーはスクロール時に固定し、コメント本文だけをスクロールする。

## Motion

状態理解に必要な場合だけ使用する。hover/focus 120ms、disclosure 160ms、dialog/popover 200msを目安とし、`cubic-bezier(.2,.8,.2,1)`を標準とする。装飾的な浮遊、発光、無限ループ、レイアウトを押し出す拡大は禁止。`prefers-reduced-motion: reduce`ではdurationを0–50msにし、spinner以外の連続アニメーションを止める。

## Density

この製品は学習コンテンツと運営作業を同時に扱うプロフェッショナルツールである。1画面に必要な情報を収めることを「圧迫」とみなさない。

- 一覧はrow40–48px、行内メタデータは1行、ラベルは短くする。
- 密な操作バーは高さ36px、要素間8px。セクション間だけ24–32pxを使う。
- 見出し直下に余分な説明を重ねず、必要な補足はsecondary 1行にする。
- 同じ状態をカード、バッジ、色、アイコンで4重に表現しない。primary表現1つ＋補助1つまで。
- 長い記事だけは本文line-height1.75と段落間24pxを許可し、管理画面へ持ち込まない。
- 画面が「均一に暗い・灰色」に見える場合は、面積を広げずprimary textと意味色の局所コントラストを上げる。色が多く見える場合は彩度を落とさず、色を使う要素数・面積を減らす。

## Responsive Design

### Desktop（980px以上）

サイドバー236px、ヘッダー64px、表は複数列、編集は本文＋補助ペインを許可する。ページpadding24px、dialog最大560px。

### Tablet（640–979px）

サイドバーは56pxまたはドロワー、header操作を折り返す。2列フォームを1列へ、表は優先列＋横スクロール。padding20px、補助ペインはタブまたは折りたたみにする。

### Mobile（639px以下）

サイドバーをドロワーにし、主要ナビは明示的なメニューボタンから開く。表は横スクロールまたは積み上げ、action列は行末に集約する。dialogは幅100%、sheetは高さ80vh以下、フォームは1列、padding16px。controlの視覚高さは32–40pxでもヒット領域44pxを維持する。横方向の本文切れ、固定幅の巨大見出しを禁止する。

## Accessibility

- 通常文字のコントラスト4.5:1以上、大きい文字・UI境界3:1以上を目標とし、意味色だけに依存しない。
- すべての操作はTab、Enter/Space、必要な矢印キーで到達・操作できる。focus ringを消さない。
- クリック領域は44×44px以上。視覚的に32pxのicon buttonでも外側hit areaで満たす。
- `button`は状態変更、`a`はURL遷移、見出しは階層順のsemantic HTMLを使う。
- `aria-current`、`aria-selected`、`aria-expanded`、`aria-pressed`、`aria-busy`、`aria-describedby`を状態に応じて付ける。
- dialogはfocus trapとfocus return、popoverはEsc終了、loadingは`aria-busy`、toastはlive regionを実装する。
- 色覚差、拡大表示、キーボードのみ、reduced motionで主要フローを完了できることを確認する。

## Content Style

- 文言は短く、ボタンは「保存」「公開」「再試行」「追加」のように動詞から始める。
- 同じ画面で用語を変えない。記事、公開、レビュー、担当など既存ドメイン語を優先する。
- 見出しと説明を重複させない。補助説明は必要な条件・結果・期限だけを書く。
- エラーは「何が起きたか／どう直すか」を1–2文で示す。責任をユーザーに押し付ける曖昧な文言を避ける。
- 数値・日時・ステータスは一覧で同じ表記・桁揃えにする。日本語UIでは日本語を基本とし、コード・URL・固有名は原文を保持する。

## Anti-Patterns

次の実装は原則として禁止する。

- 紫/青を含む装飾的グラデーション、glassmorphism、glow。
- 大面積の純黒、冷たいblue-gray、任意のTailwind Slate/Zincパレット。
- 過剰なカード化、カードの多重ネスト、カードへの常時drop shadow。
- 12pxを超えるradius、意味のないpill、巨大な丸角コンテナ。
- 強い罫線で全セルを囲む表、過度な区切り線。
- 画面全体を意味色で塗る、選択行を紫/青で塗る、色だけで状態を示す。
- 不要なバッジ、全ラベルへのアイコン、絵文字や装飾アイコンの乱用。
- 巨大見出し、マーケティング風ヒーロー、目的のない浮遊UI、過剰な余白。
- ページごとの独自control高さ、spacing、radius、色、重複コンポーネント。
- 目的を説明しないアニメーション、揺れ、点滅、無限ループ。
- 既存部品に似ているのに新しいAPI・見た目を発明すること。

## AIエージェント向け実装ルール

1. UI変更の前に必ずリポジトリルートの `DESIGN.md` を最後まで読む。
2. 既存の共通componentとCSS tokenを検索し、再利用・拡張を第一選択にする。
3. 新しい色、spacing、radius、control高さ、shadowを勝手に追加しない。必要なら本書のトークン表を先に更新する。
4. 類似componentがある場合は、寸法・状態・アクセシビリティ属性まで合わせる。
5. ページ単位で別のデザイン言語を作らない。例外は用途と移行計画をコードコメントまたはPR本文に記録する。
6. UIを派手にすること、色を増やすこと、余白を増やすことを改善とはみなさない。改善は可読性、操作性、状態理解、密度のいずれかで測る。
7. 意味色は小さい意味要素にのみ使い、neutral面・選択行・大きな背景へ拡張しない。
8. 実装後はライト／ダーク、デスクトップ／モバイル、hover／focus／disabled／errorを確認し、全主要画面で色・密度・control高さが揃っているか確認する。
9. 記事本文、記事編集、学習地図、一覧、カレンダー、応募・メンバー管理など、同じ情報構造を持つ画面で表現が分裂していないか確認する。
10. 変更にスクリーンショットまたは視覚確認が必要な場合は、主要状態をレンダリングして比較し、差分と判断理由をPRに残す。

## 実装レビュー用チェックリスト

- [ ] 本書を読み、既存component/tokenを再利用した。
- [ ] 面積の90–95%が低彩度neutralで、意味色が局所的である。
- [ ] サイドバーは主作業面より後退し、選択行に青/紫の背景がない。
- [ ] primary / secondary / tertiaryの文字階層が判別できる。
- [ ] borderは1px・低opacityで、構造を余白と面差でも伝えている。
- [ ] control高さ、radius、spacingが固定スケールに収まっている。
- [ ] hover、active、selected、focus、disabled、loading、errorを実装した。
- [ ] キーボード、ARIA、44px hit area、コントラストを確認した。
- [ ] レスポンシブ幅で表・dialog・sidebar・フォームが切れない。
- [ ] 変更後に全画面との一貫性と、既存業務フローへの影響を確認した。
